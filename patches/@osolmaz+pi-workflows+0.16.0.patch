diff --git a/node_modules/@osolmaz/pi-workflows/dist/builtins/autodoc.workflow.js b/node_modules/@osolmaz/pi-workflows/dist/builtins/autodoc.workflow.js
index 828fca1..af40a33 100644
--- a/node_modules/@osolmaz/pi-workflows/dist/builtins/autodoc.workflow.js
+++ b/node_modules/@osolmaz/pi-workflows/dist/builtins/autodoc.workflow.js
@@ -112,6 +112,11 @@ function currentPlan(context) {
         return located.plan;
     throw new Error("autodoc does not have a selected plan");
 }
+function inspectionRepository(request) {
+    return request.preparedWorkspace?.worktreePath ??
+        request.preparedWorkspace?.repository ??
+        request.repository;
+}
 function mutationRepository(request) {
     if (request.repository !== undefined)
         return request.repository;
@@ -248,8 +253,9 @@ export const autodocWorkflow = defineWorkflow({
                     "Look in the current conversation context and in the referenced canonical documents.",
                     "Do not devise, improve, replace, or implement the plan.",
                     "Return blocked when no single clear selected plan exists.",
+                    "Read repository files only under the exact absolute repository path below.",
                     `Task: ${request.task}`,
-                    `Repository: ${request.repository ?? "current repository"}`,
+                    `Repository: ${inspectionRepository(request) ?? "current repository"}`,
                     `Preferred documents: ${JSON.stringify(request.documents ?? [])}`,
                 ].join("\n");
             },
@@ -279,6 +285,8 @@ export const autodocWorkflow = defineWorkflow({
                     "Choose current only when the documents already preserve the whole selected plan and its boundaries.",
                     "Choose update when documents are missing or stale and can be corrected in scope.",
                     "Choose blocked only when no safe canonical target exists or repository rules prohibit the documentation change.",
+                    "Read and assess documentation only under the exact absolute repository path below. Do not use an equivalent file from another checkout.",
+                    `Repository: ${inspectionRepository(request) ?? "current repository"}`,
                     `Task: ${request.task}`,
                     `Selected plan: ${JSON.stringify(currentPlan(context))}`,
                     `Preferred documents: ${JSON.stringify(request.documents ?? [])}`,
diff --git a/node_modules/@osolmaz/pi-workflows/dist/extension/index.d.ts b/node_modules/@osolmaz/pi-workflows/dist/extension/index.d.ts
index b5901b1..fd02215 100644
--- a/node_modules/@osolmaz/pi-workflows/dist/extension/index.d.ts
+++ b/node_modules/@osolmaz/pi-workflows/dist/extension/index.d.ts
@@ -1,6 +1,15 @@
-import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";
+import type { ExtensionAPI, ExtensionContext } from "@earendil-works/pi-coding-agent";
+import type { HumanDecisionRequest, HumanDecisionResponse } from "../workflows/types.js";
 export { parseControllerArgs, type ParsedControllerArgs } from "./controller-command.js";
 export { PiDecisionChannel, TelegramDecisionChannel, audienceChannels, createTelegramChannels, decisionConfigDir, loadDecisionChannelConfig, verifyTelegramTokenFile, writeDecisionChannelProfile, type DecisionChannelConfig, type DecisionCredentialConfig, type HumanDecisionChannel, type HumanDecisionChannelAnswer, type HumanDecisionDeliveryResult, type LoadedDecisionChannelConfig, type PiDecisionUi, type SettledHumanDecision, type TelegramFetch, } from "./decision-channels.js";
+export type WorkflowHumanDecisionPresentation = {
+    requestId: string;
+    runId: string;
+    revision: number;
+    request: HumanDecisionRequest;
+};
+export type WorkflowHumanDecisionPresenter = (presentation: WorkflowHumanDecisionPresentation, context: ExtensionContext) => Promise<HumanDecisionResponse | undefined>;
+export declare function registerWorkflowHumanDecisionPresenter(presenter: WorkflowHumanDecisionPresenter): () => void;
 export declare function activityStateForConnection(state: "started" | "refresh" | "settled", previousConnectionId: string | null, connectionId: string): "started" | "refresh" | "settled";
 export type ParsedWorkflowArgs = {
     kind: "list";
diff --git a/node_modules/@osolmaz/pi-workflows/dist/extension/index.js b/node_modules/@osolmaz/pi-workflows/dist/extension/index.js
index abe532f..6cbdfa4 100644
--- a/node_modules/@osolmaz/pi-workflows/dist/extension/index.js
+++ b/node_modules/@osolmaz/pi-workflows/dist/extension/index.js
@@ -20,7 +20,36 @@ const INTERACTION_POLL_MS = 1_000;
 const WORKFLOW_INTERACTION_MESSAGE_TYPE = "pi-workflows-interaction";
 const WORKFLOW_NOTIFICATION_MESSAGE_TYPE = "pi-workflows-notification";
 const WORKFLOW_PRESENTATION_MESSAGE_TYPE = "pi-workflows-presentation";
+const HUMAN_DECISION_PRESENTER_KEY = Symbol.for("pi-workflows.human-decision-presenter.v1");
+const EXTERNAL_HUMAN_DECISION_PRESENTER_KEY = Symbol.for("pi-workflows.external-human-decision-presenter.v1");
 const sessionSnapshots = new Map();
+function humanDecisionPresenterRegistry() {
+    const globals = globalThis;
+    const current = globals[HUMAN_DECISION_PRESENTER_KEY];
+    if (typeof current === "object" && current !== null) {
+        return current;
+    }
+    const registry = {};
+    globals[HUMAN_DECISION_PRESENTER_KEY] = registry;
+    return registry;
+}
+export function registerWorkflowHumanDecisionPresenter(presenter) {
+    const registry = humanDecisionPresenterRegistry();
+    registry.presenter = presenter;
+    return () => {
+        if (registry.presenter === presenter)
+            registry.presenter = undefined;
+    };
+}
+function hasExternalHumanDecisionPresenter() {
+    const globals = globalThis;
+    const registry = globals[EXTERNAL_HUMAN_DECISION_PRESENTER_KEY];
+    return typeof registry === "object"
+        && registry !== null
+        && "registrations" in registry
+        && typeof registry.registrations === "number"
+        && registry.registrations > 0;
+}
 export function activityStateForConnection(state, previousConnectionId, connectionId) {
     return state === "refresh" && previousConnectionId !== connectionId ? "started" : state;
 }
@@ -153,6 +182,13 @@ export default function piWorkflows(pi) {
         });
         await prior;
         try {
+            try {
+                if (await presentPendingDecisionWithRegisteredPresenter(client, ctx))
+                    return;
+            }
+            catch (error) {
+                ctx.ui.notify(`Workflow decision popup failed: ${errorMessage(error)}`, "error");
+            }
             const deliveries = sessionSnapshots.get(ctx.sessionManager.getSessionId())?.deliveries;
             await sessionDelivery.synchronize(ctx, [
                 () => claimPendingInteractionDelivery(pi, client, ctx, startOriginActivity),
@@ -651,10 +687,82 @@ async function executeControllerCommand(client, ctx, command) {
         details: { action: command.kind, resource: response.receipt ?? null },
     };
 }
+async function presentPendingDecisionWithRegisteredPresenter(client, context) {
+    if (hasExternalHumanDecisionPresenter())
+        return false;
+    const presenter = humanDecisionPresenterRegistry().presenter;
+    if (presenter === undefined)
+        return false;
+    const sessionId = context.sessionManager.getSessionId();
+    const storedInteraction = pendingInteractionForSession(sessionId);
+    if (storedInteraction === undefined ||
+        storedInteraction.kind !== "decision" ||
+        storedInteraction.status !== "pending" ||
+        storedInteraction.presentationSessionEntryId !== null) {
+        return false;
+    }
+    const interaction = {
+        ...storedInteraction,
+        contract: await client.hydrateContent(storedInteraction.runId, storedInteraction.contract),
+    };
+    rememberInteractiveRequest(sessionId, interaction);
+    if (workflowRunPaused(interaction.runId))
+        return false;
+    if (interaction.presentationClaimExpiresAt !== null &&
+        Date.parse(interaction.presentationClaimExpiresAt) > Date.now()) {
+        return false;
+    }
+    const claimKey = `claim-presentation-${interaction.requestId}-${interaction.revision}-${client.clientId}`;
+    const claim = await client.request({
+        operation: "interaction.update",
+        requestId: claimKey,
+        idempotencyKey: claimKey,
+        runId: interaction.runId,
+        expectedRevision: interaction.revision,
+        payload: { requestId: interaction.requestId, claimPresentation: true },
+    });
+    if (claim.outcome === "conflict")
+        return false;
+    if (claim.outcome !== "accepted" && claim.outcome !== "adopted") {
+        throw new Error(claim.error ?? "Workflow host rejected interaction.update");
+    }
+    if (claim.revision === undefined)
+        throw new Error("Presentation claim has no revision");
+    rememberInteractiveRequest(sessionId, claim.receipt);
+    const contract = interactionContract(interaction);
+    if (contract.schema !== "pi-workflows.human-decision-request.v1") {
+        throw new Error("Stored workflow decision contract is invalid");
+    }
+    const response = await presenter({
+        requestId: interaction.requestId,
+        runId: interaction.runId,
+        revision: claim.revision,
+        request: contract,
+    }, context);
+    if (response === undefined)
+        return true;
+    const submissionId = randomUUID();
+    const settled = await requestAccepted(client, {
+        operation: "decision.answer",
+        requestId: `answer-${submissionId}`,
+        idempotencyKey: submissionId,
+        runId: interaction.runId,
+        expectedRevision: claim.revision,
+        payload: {
+            requestId: interaction.requestId,
+            submissionId,
+            response: decisionResponse(response),
+        },
+    });
+    rememberInteractiveRequest(sessionId, settled.receipt);
+    return true;
+}
 async function claimPendingInteractionDelivery(pi, client, ctx, onPresented) {
     const storedInteraction = pendingInteractionForSession(ctx.sessionManager.getSessionId());
     if (storedInteraction === undefined || storedInteraction.presentationSessionEntryId !== null)
         return undefined;
+    if (storedInteraction.kind === "decision" && hasExternalHumanDecisionPresenter())
+        return undefined;
     const interaction = {
         ...storedInteraction,
         contract: await client.hydrateContent(storedInteraction.runId, storedInteraction.contract),
diff --git a/node_modules/@osolmaz/pi-workflows/dist/host/runner.d.ts b/node_modules/@osolmaz/pi-workflows/dist/host/runner.d.ts
index dd34ef8..5eaa280 100644
--- a/node_modules/@osolmaz/pi-workflows/dist/host/runner.d.ts
+++ b/node_modules/@osolmaz/pi-workflows/dist/host/runner.d.ts
@@ -1,4 +1,10 @@
+import type { WorkflowRunState } from "../workflows/types.js";
 import { HostProcessRegistry } from "./processes.js";
+export declare function workerResumePayload(loaded: {
+    state: WorkflowRunState;
+}): {
+    state: WorkflowRunState;
+};
 export type WorkflowHostOptions = {
     /** Retained for callers; the host itself is global to the state database. */
     cwd?: string;
diff --git a/node_modules/@osolmaz/pi-workflows/dist/host/runner.js b/node_modules/@osolmaz/pi-workflows/dist/host/runner.js
index fbc8bbc..0c5948e 100644
--- a/node_modules/@osolmaz/pi-workflows/dist/host/runner.js
+++ b/node_modules/@osolmaz/pi-workflows/dist/host/runner.js
@@ -34,6 +34,9 @@ const CONTROLLER_CLAIM_LEASE_MS = 120_000;
 const CONTROLLER_RENEW_MS = 30_000;
 const MAX_CONTROLLER_WORKERS = 4;
 const DEFAULT_CONTROLLER_TIMEOUT_MS = 60_000;
+export function workerResumePayload(loaded) {
+    return { state: loaded.state };
+}
 /** Global package-owned host. It writes state and supervises code-only workers. */
 export class WorkflowHost {
     options;
@@ -2172,7 +2175,7 @@ export class WorkflowHost {
                     break;
                 case "store.prepareRunResume":
                     await this.prepareInteractionResume(message.runId);
-                    result = await this.runStore.prepareRunResume(message.runId);
+                    result = workerResumePayload(await this.runStore.prepareRunResume(message.runId));
                     break;
                 case "store.readRun":
                     result = this.runStore.readRun(requireString(payload.runId, "runId"), payload.options);
diff --git a/node_modules/@osolmaz/pi-workflows/dist/host/view.d.ts b/node_modules/@osolmaz/pi-workflows/dist/host/view.d.ts
index bbc7753..be651f9 100644
--- a/node_modules/@osolmaz/pi-workflows/dist/host/view.d.ts
+++ b/node_modules/@osolmaz/pi-workflows/dist/host/view.d.ts
@@ -1,4 +1,4 @@
-import { type OriginActivityReport, type WorkflowDisplay, type WorkflowRunListPage, type WorkflowRunView, type WorkflowSessionView } from "../client/view.js";
+import { type OriginActivityReport, type WorkflowDisplay, type WorkflowDisplayStatus, type WorkflowRunListPage, type WorkflowRunView, type WorkflowSessionView } from "../client/view.js";
 import type { SqliteControllerStore, WorkflowRunQueueRecord } from "../controllers/sqlite.js";
 import type { StateDatabase } from "../state/database.js";
 import { type JsonValue } from "../state/json.js";
@@ -8,6 +8,7 @@ import type { HostStateStore } from "./state.js";
 export type { OriginActivityReport } from "../client/view.js";
 export declare const WORKFLOW_PAGE_KINDS: readonly ["steps", "trace", "trace_at_step", "session_entries", "session_events", "settings", "follow_ups", "updates"];
 export type WorkflowPageKind = (typeof WORKFLOW_PAGE_KINDS)[number];
+export declare function possiblyInterrupted(queueStatus: WorkflowRunQueueRecord["status"], displayStatus: WorkflowDisplayStatus): boolean;
 type RunPageRequest = {
     kind: WorkflowPageKind;
     cursor: number;
diff --git a/node_modules/@osolmaz/pi-workflows/dist/host/view.js b/node_modules/@osolmaz/pi-workflows/dist/host/view.js
index e5d258b..18502a9 100644
--- a/node_modules/@osolmaz/pi-workflows/dist/host/view.js
+++ b/node_modules/@osolmaz/pi-workflows/dist/host/view.js
@@ -18,6 +18,9 @@ const VIEW_PAGE_ITEMS = 256;
 const CONTENT_CHUNK_BYTES = 192 * 1024;
 const CONTENT_CACHE_BYTES = 64 * 1024 * 1024;
 const VIEW_CACHE_ITEMS = 64;
+export function possiblyInterrupted(queueStatus, displayStatus) {
+    return queueStatus === "parked" && displayStatus === "queued";
+}
 export class HostViewStore {
     state;
     queue;
@@ -66,7 +69,7 @@ export class HostViewStore {
                     display,
                     manifest: manifest(run, display.status),
                     live: display.status === "running" || display.status === "waiting",
-                    possiblyInterrupted: run.status === "parked" && display.status !== "paused",
+                    possiblyInterrupted: possiblyInterrupted(run.status, display.status),
                 };
             });
             const items = byteBoundedForwardPage(summaries, (summary) => toJson(summary)).map((item) => item);
@@ -191,7 +194,7 @@ export class HostViewStore {
             updateStart: updatePage.start,
             updateTotal: updatePage.total,
             live: display.status === "running" || display.status === "waiting",
-            possiblyInterrupted: queue.status === "parked" && display.status !== "paused",
+            possiblyInterrupted: possiblyInterrupted(queue.status, display.status),
         };
     }
     session(sessionId) {
diff --git a/node_modules/@osolmaz/pi-workflows/dist/host/worker-protocol.js b/node_modules/@osolmaz/pi-workflows/dist/host/worker-protocol.js
index 7dd9b23..6e20dcf 100644
--- a/node_modules/@osolmaz/pi-workflows/dist/host/worker-protocol.js
+++ b/node_modules/@osolmaz/pi-workflows/dist/host/worker-protocol.js
@@ -1,7 +1,10 @@
+import { gunzipSync, gzipSync } from "node:zlib";
 import { MAX_PROTOCOL_MESSAGE_BYTES } from "../client/protocol.js";
 import { canonicalJson, parseJson } from "../state/json.js";
 export const WORKER_MESSAGE_SCHEMA = "pi-workflows.worker-message.v1";
 export const WORKER_RESPONSE_SCHEMA = "pi-workflows.worker-response.v1";
+const COMPRESSED_WORKER_FRAME_SCHEMA = "pi-workflows.worker-compressed.v1";
+const MAX_UNCOMPRESSED_WORKER_MESSAGE_BYTES = 8 * 1024 * 1024;
 export const WORKER_MESSAGE_KINDS = [
     "worker.ready",
     "node.started",
@@ -55,9 +58,22 @@ export function workerKindForOperation(operation, payload) {
     return "worker.progress";
 }
 export function encodeWorkerLine(message) {
-    const encoded = Buffer.from(`${canonicalJson(message)}\n`, "utf8");
+    const messageBuffer = Buffer.from(canonicalJson(message), "utf8");
+    if (messageBuffer.byteLength + 1 <= MAX_PROTOCOL_MESSAGE_BYTES) {
+        return Buffer.concat([messageBuffer, Buffer.from("\n")]);
+    }
+    if (messageBuffer.byteLength > MAX_UNCOMPRESSED_WORKER_MESSAGE_BYTES) {
+        throw new Error("Uncompressed worker protocol message exceeds 8 MiB");
+    }
+    const compressed = gzipSync(messageBuffer).toString("base64");
+    const encoded = Buffer.from(`${canonicalJson({
+        schema: COMPRESSED_WORKER_FRAME_SCHEMA,
+        encoding: "gzip+base64",
+        uncompressedBytes: messageBuffer.byteLength,
+        data: compressed,
+    })}\n`, "utf8");
     if (encoded.byteLength > MAX_PROTOCOL_MESSAGE_BYTES) {
-        throw new Error("Worker protocol message exceeds 1 MiB");
+        throw new Error("Compressed worker protocol message exceeds 1 MiB");
     }
     return encoded;
 }
@@ -104,6 +120,42 @@ function parseWorkerValue(line) {
     if (buffer.byteLength + 1 > MAX_PROTOCOL_MESSAGE_BYTES) {
         throw new Error("Worker protocol message exceeds 1 MiB");
     }
+    const value = parseCanonicalWorkerObject(buffer);
+    if (value.schema !== COMPRESSED_WORKER_FRAME_SCHEMA)
+        return value;
+    if (value.encoding !== "gzip+base64" ||
+        !Number.isSafeInteger(value.uncompressedBytes) ||
+        value.uncompressedBytes <= 0 ||
+        value.uncompressedBytes > MAX_UNCOMPRESSED_WORKER_MESSAGE_BYTES ||
+        typeof value.data !== "string" ||
+        Object.keys(value).sort().join(",") !== "data,encoding,schema,uncompressedBytes") {
+        throw new Error("Invalid compressed worker protocol message");
+    }
+    try {
+        const compressed = Buffer.from(value.data, "base64");
+        if (compressed.toString("base64") !== value.data) {
+            throw new Error("Compressed worker protocol payload must use canonical base64");
+        }
+        const decoded = gunzipSync(compressed, {
+            maxOutputLength: MAX_UNCOMPRESSED_WORKER_MESSAGE_BYTES,
+        });
+        if (decoded.byteLength !== value.uncompressedBytes) {
+            throw new Error("Compressed worker protocol length mismatch");
+        }
+        const decodedValue = parseCanonicalWorkerObject(decoded);
+        if (decodedValue.schema === COMPRESSED_WORKER_FRAME_SCHEMA) {
+            throw new Error("Nested compressed worker protocol messages are not supported");
+        }
+        return decodedValue;
+    }
+    catch (error) {
+        if (error instanceof Error && error.message.startsWith("Compressed worker protocol")) {
+            throw error;
+        }
+        throw new Error("Invalid compressed worker protocol payload");
+    }
+}
+function parseCanonicalWorkerObject(buffer) {
     const text = buffer.toString("utf8");
     const value = parseJson(text);
     if (!isRecord(value) || canonicalJson(value) !== text) {
diff --git a/node_modules/@osolmaz/pi-workflows/dist/host/worker-store.js b/node_modules/@osolmaz/pi-workflows/dist/host/worker-store.js
index 9d68505..ce5437f 100644
--- a/node_modules/@osolmaz/pi-workflows/dist/host/worker-store.js
+++ b/node_modules/@osolmaz/pi-workflows/dist/host/worker-store.js
@@ -33,17 +33,19 @@ export class HostBackedWorkflowStore {
         return await this.call("store.publishUpdate", { runId, state, nodeId, attemptId, update }, attemptId);
     }
     async findSettingsScope(runId, mountPath, invocation) {
-        return await this.call("store.findSettingsScope", {
+        const scope = await this.call("store.findSettingsScope", {
             runId,
             mountPath,
             invocation,
         });
+        return scope === null ? undefined : scope;
     }
     async ensureSettingsScope(options) {
         return await this.call("store.ensureSettingsScope", { options });
     }
     async getSettingsScopeAtChange(scopeId, changeNumber) {
-        return await this.call("store.getSettingsScopeAtChange", { scopeId, changeNumber });
+        const scope = await this.call("store.getSettingsScopeAtChange", { scopeId, changeNumber });
+        return scope === null ? undefined : scope;
     }
     async createHumanDecisionRequest(request) {
         return await this.call("store.createHumanDecisionRequest", { request }, request.attemptId);
diff --git a/node_modules/@osolmaz/pi-workflows/dist/workflows/store.js b/node_modules/@osolmaz/pi-workflows/dist/workflows/store.js
index 33aceb9..25cea11 100644
--- a/node_modules/@osolmaz/pi-workflows/dist/workflows/store.js
+++ b/node_modules/@osolmaz/pi-workflows/dist/workflows/store.js
@@ -320,6 +320,11 @@ export class WorkflowRunStore {
                         payload: { workflowName },
                     };
                     insertRunEvent(this.state, reserved.resourceId, revision, at, traceEvent);
+                    // A reserved continuation row was queued with a placeholder input; persist the
+                    // engine's real input so later worker generations read the same state.
+                    this.state.connection
+                      .prepare("UPDATE runs SET input_hash = ? WHERE run_id = ?")
+                      .run(this.state.putJson(state.input, now), state.runId);
                     this.persistRunState(reserved, state, revision, now);
                     initializeViewerRun(this.state, state.runId, now);
                     this.initializeRunSettingsAndFollowUps(state, options.initialSettings ?? [], now);
diff --git a/node_modules/@osolmaz/pi-workflows/src/builtins/autodoc.workflow.ts b/node_modules/@osolmaz/pi-workflows/src/builtins/autodoc.workflow.ts
index a3d34be..a2148f9 100644
--- a/node_modules/@osolmaz/pi-workflows/src/builtins/autodoc.workflow.ts
+++ b/node_modules/@osolmaz/pi-workflows/src/builtins/autodoc.workflow.ts
@@ -191,6 +191,14 @@ function currentPlan(context: WorkflowNodeContext): unknown {
   throw new Error("autodoc does not have a selected plan");
 }

+function inspectionRepository(request: AutodocInput): string | undefined {
+  return (
+    request.preparedWorkspace?.worktreePath
+    ?? request.preparedWorkspace?.repository
+    ?? request.repository
+  );
+}
+
 function mutationRepository(request: AutodocInput): string {
   if (request.repository !== undefined) return request.repository;
   if (request.preparedWorkspace !== undefined) return request.preparedWorkspace.repository;
@@ -329,8 +337,9 @@ export const autodocWorkflow = defineWorkflow({
           "Look in the current conversation context and in the referenced canonical documents.",
           "Do not devise, improve, replace, or implement the plan.",
           "Return blocked when no single clear selected plan exists.",
+          "Read repository files only under the exact absolute repository path below.",
           `Task: ${request.task}`,
-          `Repository: ${request.repository ?? "current repository"}`,
+          `Repository: ${inspectionRepository(request) ?? "current repository"}`,
           `Preferred documents: ${JSON.stringify(request.documents ?? [])}`,
         ].join("\n");
       },
@@ -361,6 +370,8 @@ export const autodocWorkflow = defineWorkflow({
           "Choose current only when the documents already preserve the whole selected plan and its boundaries.",
           "Choose update when documents are missing or stale and can be corrected in scope.",
           "Choose blocked only when no safe canonical target exists or repository rules prohibit the documentation change.",
+          "Read and assess documentation only under the exact absolute repository path below. Do not use an equivalent file from another checkout.",
+          `Repository: ${inspectionRepository(request) ?? "current repository"}`,
           `Task: ${request.task}`,
           `Selected plan: ${JSON.stringify(currentPlan(context))}`,
           `Preferred documents: ${JSON.stringify(request.documents ?? [])}`,
diff --git a/node_modules/@osolmaz/pi-workflows/src/extension/index.ts b/node_modules/@osolmaz/pi-workflows/src/extension/index.ts
index a990e7e..030cacd 100644
--- a/node_modules/@osolmaz/pi-workflows/src/extension/index.ts
+++ b/node_modules/@osolmaz/pi-workflows/src/extension/index.ts
@@ -15,7 +15,11 @@ import { canonicalJson, parseJson, type JsonValue } from "../state/json.js";
 import { errorMessage } from "../workflows/errors.js";
 import { discoverWorkflows } from "../workflows/loader.js";
 import { createRunId } from "../workflows/store.js";
-import type { AgentStepContract, HumanDecisionResponse } from "../workflows/types.js";
+import type {
+  AgentStepContract,
+  HumanDecisionRequest,
+  HumanDecisionResponse,
+} from "../workflows/types.js";
 import { parseControllerArgs, type ParsedControllerArgs } from "./controller-command.js";
 import {
   HerdrWorkflowViewer,
@@ -61,8 +65,63 @@ const INTERACTION_POLL_MS = 1_000;
 const WORKFLOW_INTERACTION_MESSAGE_TYPE = "pi-workflows-interaction";
 const WORKFLOW_NOTIFICATION_MESSAGE_TYPE = "pi-workflows-notification";
 const WORKFLOW_PRESENTATION_MESSAGE_TYPE = "pi-workflows-presentation";
+const HUMAN_DECISION_PRESENTER_KEY = Symbol.for("pi-workflows.human-decision-presenter.v1");
+const EXTERNAL_HUMAN_DECISION_PRESENTER_KEY = Symbol.for(
+  "pi-workflows.external-human-decision-presenter.v1",
+);
 const sessionSnapshots = new Map<string, WorkflowSessionView>();

+export type WorkflowHumanDecisionPresentation = {
+  requestId: string;
+  runId: string;
+  revision: number;
+  request: HumanDecisionRequest;
+};
+
+export type WorkflowHumanDecisionPresenter = (
+  presentation: WorkflowHumanDecisionPresentation,
+  context: ExtensionContext,
+) => Promise<HumanDecisionResponse | undefined>;
+
+type HumanDecisionPresenterRegistry = {
+  presenter?: WorkflowHumanDecisionPresenter;
+};
+
+function humanDecisionPresenterRegistry(): HumanDecisionPresenterRegistry {
+  const globals = globalThis as typeof globalThis & {
+    [HUMAN_DECISION_PRESENTER_KEY]?: unknown;
+  };
+  const current = globals[HUMAN_DECISION_PRESENTER_KEY];
+  if (typeof current === "object" && current !== null) {
+    return current as HumanDecisionPresenterRegistry;
+  }
+  const registry: HumanDecisionPresenterRegistry = {};
+  globals[HUMAN_DECISION_PRESENTER_KEY] = registry;
+  return registry;
+}
+
+export function registerWorkflowHumanDecisionPresenter(
+  presenter: WorkflowHumanDecisionPresenter,
+): () => void {
+  const registry = humanDecisionPresenterRegistry();
+  registry.presenter = presenter;
+  return () => {
+    if (registry.presenter === presenter) registry.presenter = undefined;
+  };
+}
+
+function hasExternalHumanDecisionPresenter(): boolean {
+  const globals = globalThis as typeof globalThis & {
+    [EXTERNAL_HUMAN_DECISION_PRESENTER_KEY]?: unknown;
+  };
+  const registry = globals[EXTERNAL_HUMAN_DECISION_PRESENTER_KEY];
+  return typeof registry === "object"
+    && registry !== null
+    && "registrations" in registry
+    && typeof registry.registrations === "number"
+    && registry.registrations > 0;
+}
+
 type PresentedOriginActivity = {
   sessionId: string;
   runId: string;
@@ -222,6 +281,11 @@ export default function piWorkflows(pi: ExtensionAPI): void {
     });
     await prior;
     try {
+      try {
+        if (await presentPendingDecisionWithRegisteredPresenter(client, ctx)) return;
+      } catch (error) {
+        ctx.ui.notify(`Workflow decision popup failed: ${errorMessage(error)}`, "error");
+      }
       const deliveries = sessionSnapshots.get(ctx.sessionManager.getSessionId())?.deliveries;
       await sessionDelivery.synchronize(ctx, [
         () => claimPendingInteractionDelivery(pi, client, ctx, startOriginActivity),
@@ -764,6 +828,87 @@ async function executeControllerCommand(
   };
 }

+async function presentPendingDecisionWithRegisteredPresenter(
+  client: WorkflowClient,
+  context: ExtensionContext,
+): Promise<boolean> {
+  if (hasExternalHumanDecisionPresenter()) return false;
+  const presenter = humanDecisionPresenterRegistry().presenter;
+  if (presenter === undefined) return false;
+
+  const sessionId = context.sessionManager.getSessionId();
+  const storedInteraction = pendingInteractionForSession(sessionId);
+  if (
+    storedInteraction === undefined ||
+    storedInteraction.kind !== "decision" ||
+    storedInteraction.status !== "pending" ||
+    storedInteraction.presentationSessionEntryId !== null
+  ) {
+    return false;
+  }
+  const interaction = {
+    ...storedInteraction,
+    contract: await client.hydrateContent(storedInteraction.runId, storedInteraction.contract),
+  };
+  rememberInteractiveRequest(sessionId, interaction);
+
+  if (workflowRunPaused(interaction.runId)) return false;
+  if (
+    interaction.presentationClaimExpiresAt !== null &&
+    Date.parse(interaction.presentationClaimExpiresAt) > Date.now()
+  ) {
+    return false;
+  }
+
+  const claimKey =
+    `claim-presentation-${interaction.requestId}-${interaction.revision}-${client.clientId}`;
+  const claim = await client.request({
+    operation: "interaction.update",
+    requestId: claimKey,
+    idempotencyKey: claimKey,
+    runId: interaction.runId,
+    expectedRevision: interaction.revision,
+    payload: { requestId: interaction.requestId, claimPresentation: true },
+  });
+  if (claim.outcome === "conflict") return false;
+  if (claim.outcome !== "accepted" && claim.outcome !== "adopted") {
+    throw new Error(claim.error ?? "Workflow host rejected interaction.update");
+  }
+  if (claim.revision === undefined) throw new Error("Presentation claim has no revision");
+  rememberInteractiveRequest(sessionId, claim.receipt);
+
+  const contract = interactionContract(interaction);
+  if (contract.schema !== "pi-workflows.human-decision-request.v1") {
+    throw new Error("Stored workflow decision contract is invalid");
+  }
+  const response = await presenter(
+    {
+      requestId: interaction.requestId,
+      runId: interaction.runId,
+      revision: claim.revision,
+      request: contract as unknown as HumanDecisionRequest,
+    },
+    context,
+  );
+  if (response === undefined) return true;
+
+  const submissionId = randomUUID();
+  const settled = await requestAccepted(client, {
+    operation: "decision.answer",
+    requestId: `answer-${submissionId}`,
+    idempotencyKey: submissionId,
+    runId: interaction.runId,
+    expectedRevision: claim.revision,
+    payload: {
+      requestId: interaction.requestId,
+      submissionId,
+      response: decisionResponse(response),
+    },
+  });
+  rememberInteractiveRequest(sessionId, settled.receipt);
+  return true;
+}
+
 async function claimPendingInteractionDelivery(
   pi: ExtensionAPI,
   client: WorkflowClient,
@@ -773,6 +918,7 @@ async function claimPendingInteractionDelivery(
   const storedInteraction = pendingInteractionForSession(ctx.sessionManager.getSessionId());
   if (storedInteraction === undefined || storedInteraction.presentationSessionEntryId !== null)
     return undefined;
+  if (storedInteraction.kind === "decision" && hasExternalHumanDecisionPresenter()) return undefined;
   const interaction = {
     ...storedInteraction,
     contract: await client.hydrateContent(storedInteraction.runId, storedInteraction.contract),
diff --git a/node_modules/@osolmaz/pi-workflows/src/host/runner.ts b/node_modules/@osolmaz/pi-workflows/src/host/runner.ts
index 4569ea9..0b85576 100644
--- a/node_modules/@osolmaz/pi-workflows/src/host/runner.ts
+++ b/node_modules/@osolmaz/pi-workflows/src/host/runner.ts
@@ -102,6 +102,12 @@ const CONTROLLER_RENEW_MS = 30_000;
 const MAX_CONTROLLER_WORKERS = 4;
 const DEFAULT_CONTROLLER_TIMEOUT_MS = 60_000;

+export function workerResumePayload(loaded: { state: WorkflowRunState }): {
+  state: WorkflowRunState;
+} {
+  return { state: loaded.state };
+}
+
 export type WorkflowHostOptions = {
   /** Retained for callers; the host itself is global to the state database. */
   cwd?: string;
@@ -2602,7 +2608,7 @@ export class WorkflowHost {
           break;
         case "store.prepareRunResume":
           await this.prepareInteractionResume(message.runId);
-          result = await this.runStore.prepareRunResume(message.runId);
+          result = workerResumePayload(await this.runStore.prepareRunResume(message.runId));
           break;
         case "store.readRun":
           result = this.runStore.readRun(
diff --git a/node_modules/@osolmaz/pi-workflows/src/host/view.ts b/node_modules/@osolmaz/pi-workflows/src/host/view.ts
index fd36986..2d3d002 100644
--- a/node_modules/@osolmaz/pi-workflows/src/host/view.ts
+++ b/node_modules/@osolmaz/pi-workflows/src/host/view.ts
@@ -70,6 +70,13 @@ const CONTENT_CHUNK_BYTES = 192 * 1024;
 const CONTENT_CACHE_BYTES = 64 * 1024 * 1024;
 const VIEW_CACHE_ITEMS = 64;

+export function possiblyInterrupted(
+  queueStatus: WorkflowRunQueueRecord["status"],
+  displayStatus: WorkflowDisplayStatus,
+): boolean {
+  return queueStatus === "parked" && displayStatus === "queued";
+}
+
 export class HostViewStore {
   private readonly activity = new Map<string, OriginActivity>();
   private readonly contentRecords = new Map<string, ContentRecord>();
@@ -118,7 +125,7 @@ export class HostViewStore {
           display,
           manifest: manifest(run, display.status),
           live: display.status === "running" || display.status === "waiting",
-          possiblyInterrupted: run.status === "parked" && display.status !== "paused",
+          possiblyInterrupted: possiblyInterrupted(run.status, display.status),
         } satisfies WorkflowRunSummary;
       });
       const items = byteBoundedForwardPage(summaries, (summary) => toJson(summary)).map(
@@ -314,7 +321,7 @@ export class HostViewStore {
       updateStart: updatePage.start,
       updateTotal: updatePage.total,
       live: display.status === "running" || display.status === "waiting",
-      possiblyInterrupted: queue.status === "parked" && display.status !== "paused",
+      possiblyInterrupted: possiblyInterrupted(queue.status, display.status),
     };
   }

diff --git a/node_modules/@osolmaz/pi-workflows/src/host/worker-protocol.ts b/node_modules/@osolmaz/pi-workflows/src/host/worker-protocol.ts
index c26abe8..0c25747 100644
--- a/node_modules/@osolmaz/pi-workflows/src/host/worker-protocol.ts
+++ b/node_modules/@osolmaz/pi-workflows/src/host/worker-protocol.ts
@@ -1,8 +1,11 @@
+import { gunzipSync, gzipSync } from "node:zlib";
 import { MAX_PROTOCOL_MESSAGE_BYTES } from "../client/protocol.js";
 import { canonicalJson, parseJson, type JsonValue } from "../state/json.js";

 export const WORKER_MESSAGE_SCHEMA = "pi-workflows.worker-message.v1" as const;
 export const WORKER_RESPONSE_SCHEMA = "pi-workflows.worker-response.v1" as const;
+const COMPRESSED_WORKER_FRAME_SCHEMA = "pi-workflows.worker-compressed.v1" as const;
+const MAX_UNCOMPRESSED_WORKER_MESSAGE_BYTES = 8 * 1024 * 1024;

 export const WORKER_MESSAGE_KINDS = [
   "worker.ready",
@@ -95,9 +98,25 @@ export function workerKindForOperation(
 }

 export function encodeWorkerLine(message: WorkerMessage | WorkerResponse): Buffer {
-  const encoded = Buffer.from(`${canonicalJson(message)}\n`, "utf8");
+  const messageBuffer = Buffer.from(canonicalJson(message), "utf8");
+  if (messageBuffer.byteLength + 1 <= MAX_PROTOCOL_MESSAGE_BYTES) {
+    return Buffer.concat([messageBuffer, Buffer.from("\n")]);
+  }
+  if (messageBuffer.byteLength > MAX_UNCOMPRESSED_WORKER_MESSAGE_BYTES) {
+    throw new Error("Uncompressed worker protocol message exceeds 8 MiB");
+  }
+  const compressed = gzipSync(messageBuffer).toString("base64");
+  const encoded = Buffer.from(
+    `${canonicalJson({
+      schema: COMPRESSED_WORKER_FRAME_SCHEMA,
+      encoding: "gzip+base64",
+      uncompressedBytes: messageBuffer.byteLength,
+      data: compressed,
+    })}\n`,
+    "utf8",
+  );
   if (encoded.byteLength > MAX_PROTOCOL_MESSAGE_BYTES) {
-    throw new Error("Worker protocol message exceeds 1 MiB");
+    throw new Error("Compressed worker protocol message exceeds 1 MiB");
   }
   return encoded;
 }
@@ -146,6 +165,43 @@ function parseWorkerValue(line: Buffer | string): Record<string, unknown> {
   if (buffer.byteLength + 1 > MAX_PROTOCOL_MESSAGE_BYTES) {
     throw new Error("Worker protocol message exceeds 1 MiB");
   }
+  const value = parseCanonicalWorkerObject(buffer);
+  if (value.schema !== COMPRESSED_WORKER_FRAME_SCHEMA) return value;
+  if (
+    value.encoding !== "gzip+base64" ||
+    !Number.isSafeInteger(value.uncompressedBytes) ||
+    (value.uncompressedBytes as number) <= 0 ||
+    (value.uncompressedBytes as number) > MAX_UNCOMPRESSED_WORKER_MESSAGE_BYTES ||
+    typeof value.data !== "string" ||
+    Object.keys(value).sort().join(",") !== "data,encoding,schema,uncompressedBytes"
+  ) {
+    throw new Error("Invalid compressed worker protocol message");
+  }
+  try {
+    const compressed = Buffer.from(value.data, "base64");
+    if (compressed.toString("base64") !== value.data) {
+      throw new Error("Compressed worker protocol payload must use canonical base64");
+    }
+    const decoded = gunzipSync(compressed, {
+      maxOutputLength: MAX_UNCOMPRESSED_WORKER_MESSAGE_BYTES,
+    });
+    if (decoded.byteLength !== value.uncompressedBytes) {
+      throw new Error("Compressed worker protocol length mismatch");
+    }
+    const decodedValue = parseCanonicalWorkerObject(decoded);
+    if (decodedValue.schema === COMPRESSED_WORKER_FRAME_SCHEMA) {
+      throw new Error("Nested compressed worker protocol messages are not supported");
+    }
+    return decodedValue;
+  } catch (error) {
+    if (error instanceof Error && error.message.startsWith("Compressed worker protocol")) {
+      throw error;
+    }
+    throw new Error("Invalid compressed worker protocol payload");
+  }
+}
+
+function parseCanonicalWorkerObject(buffer: Buffer): Record<string, unknown> {
   const text = buffer.toString("utf8");
   const value = parseJson(text);
   if (!isRecord(value) || canonicalJson(value) !== text) {
diff --git a/node_modules/@osolmaz/pi-workflows/src/host/worker-store.ts b/node_modules/@osolmaz/pi-workflows/src/host/worker-store.ts
index 5d76c77..6d89260 100644
--- a/node_modules/@osolmaz/pi-workflows/src/host/worker-store.ts
+++ b/node_modules/@osolmaz/pi-workflows/src/host/worker-store.ts
@@ -104,11 +104,11 @@ export class HostBackedWorkflowStore implements WorkflowExecutionStore {
     mountPath: string,
     invocation: number,
   ): Promise<WorkflowSettingsScopeRecord | undefined> {
-    return await this.call<WorkflowSettingsScopeRecord | undefined>("store.findSettingsScope", {
-      runId,
-      mountPath,
-      invocation,
-    });
+    const scope = await this.call<WorkflowSettingsScopeRecord | null>(
+      "store.findSettingsScope",
+      { runId, mountPath, invocation },
+    );
+    return scope === null ? undefined : scope;
   }

   async ensureSettingsScope(options: {
@@ -124,10 +124,11 @@ export class HostBackedWorkflowStore implements WorkflowExecutionStore {
     scopeId: string,
     changeNumber: number,
   ): Promise<WorkflowSettingsScopeRecord | undefined> {
-    return await this.call<WorkflowSettingsScopeRecord | undefined>(
+    const scope = await this.call<WorkflowSettingsScopeRecord | null>(
       "store.getSettingsScopeAtChange",
       { scopeId, changeNumber },
     );
+    return scope === null ? undefined : scope;
   }

   async createHumanDecisionRequest(request: HumanDecisionRequest): Promise<"created" | "adopted"> {
diff --git a/node_modules/@osolmaz/pi-workflows/src/workflows/store.ts b/node_modules/@osolmaz/pi-workflows/src/workflows/store.ts
index f0f87eb..cbacade 100644
--- a/node_modules/@osolmaz/pi-workflows/src/workflows/store.ts
+++ b/node_modules/@osolmaz/pi-workflows/src/workflows/store.ts
@@ -889,6 +889,11 @@ export class WorkflowRunStore {
             payload: { workflowName },
           };
           insertRunEvent(this.state, reserved.resourceId, revision, at, traceEvent);
+          // A reserved continuation row was queued with a placeholder input; persist the
+          // engine's real input so later worker generations read the same state.
+          this.state.connection
+            .prepare("UPDATE runs SET input_hash = ? WHERE run_id = ?")
+            .run(this.state.putJson(state.input, now), state.runId);
           this.persistRunState(reserved, state, revision, now);
           initializeViewerRun(this.state, state.runId, now);
           this.initializeRunSettingsAndFollowUps(state, options.initialSettings ?? [], now);
