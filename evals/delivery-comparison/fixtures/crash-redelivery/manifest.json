{
  "id": "crash-redelivery",
  "capability": "crash_redelivery",
  "instruction": "Make apply_delivery idempotent by stable action_id so redelivery after a crash between dispatch and acknowledgement cannot apply the increment twice. Keep the public API unchanged.",
  "setup_argv": ["python", "-m", "unittest", "discover", "-v"],
  "reset_strategy": "fresh_copy_and_git_init",
  "visible_test_argv": ["python", "-m", "unittest", "discover", "-v"],
  "allowed_write_paths": ["worker.py"],
  "protected_paths": ["test_worker.py"],
  "expected_final_behavior": ["repeated action_id is a no-op", "distinct action IDs apply independently"],
  "accepted_equivalent_outcomes": ["Any at-most-once implementation retaining total and applied action IDs"],
  "fault_point": "after-dispatch-before-ack"
}
